name: exam_attendance
description: "تطبيق تسجيل حضور الطلاب بالباركود للامتحانات"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  mobile_scanner: ^5.1.1
  excel: ^4.0.3
  file_picker: ^8.0.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  intl: ^0.19.0
  vibration: ^1.8.4
  provider: ^6.1.2
  share_plus: ^9.0.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
